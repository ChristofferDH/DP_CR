import numpy as np
import HelperFunctions as func


def EGMUE(sol, t, par):
    # EGM and upper envelope

    pass

    for j in range(par.m_N):
        v[j] = -np.inf

    for i in range(par.a_N):
        w[i] =
        c[i] =
        m[i] = a[i] + c[i] 
    

    return sol